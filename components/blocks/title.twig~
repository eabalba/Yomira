{% set className = className|default('title block--title') %}
{% set subtitle = fields.subtitle|default(subtitle) %}
{% set title = fields.title|default(title) %}

<section id="{{ id }}" class="{{ className }}">
  <div class="container">
    <div class="row">
      <div class="col">
        <div class="title__contents">
          {% if title %}
            <h1>
              {{ title }}
            </h1>
          {% endif %}
          {% if subtitle %}
            <h2>
              {{ subtitle }}
            </h2>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</section>
